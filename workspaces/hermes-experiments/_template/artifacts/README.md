# Artifacts

Store curated outputs worth preserving: scripts, generated files, eval JSON, screenshots, prompt contracts, or concise result summaries.

Avoid dumping bulky/private temporary files here unless the experiment explicitly needs them and they are safe to commit.
